# 2026春数据挖掘与机器学习考试回忆

# 选择题

绝大多数都在题库

没在的有

1. 频繁项集的**Apriori 算法**。
2. 关联规则类似于{牛奶,面包}⇒{鸡蛋}的含义

# 计算题

计算信息熵

# 分析题

1. **真实标签概率为 P(A)=0，P(B)=1，P(C)=0；模型预测概率为 q(A)=0.228，q(B)=0.619，q(C)=0.153**
   1. 计算交叉熵损失
   2. 替换一个数之后计算交叉熵 结合交叉熵含义说明含义(?)
   3. 如果q(B)=0.9 交叉熵是上升还是下降为什么
2. 给了一组{x_1,x_2,属性}
   1. 计算欧几里得距离，说明KNN的K=3时，投票结果是什么
   2. 计算曼哈顿距离 说明KNN的K=3时，投票结果是什么
   3. 为什么需要归一化 不归一化会发生什么

# 问答题

大概是对机器学习上有什么看法之类的

tip：一定要看看计算器有没有算log不同底的 考试在计算时会要求以2为底或者其他

换底公式: $log_{a}b=\frac{log_{c}b}{log_{c}a}$
